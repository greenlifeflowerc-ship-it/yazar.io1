# Premium skins

Drop `.png`, `.jpg`, `.jpeg`, or `.webp` files into this folder. They will
appear automatically in the **Premium** tab of the in-game skin chooser.

Same rules as `assets/skins/level/` — square images, transparent backgrounds
optional, any reasonable filename.
